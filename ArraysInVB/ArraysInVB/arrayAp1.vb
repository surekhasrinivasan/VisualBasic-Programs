Module arrayAp1
    ' Program to show the elements in an array can be stored and accessed by using the Index of the array
    Sub Main()
        ' n is an array of 11 integers 
        Dim n(10) As Integer

        '  initialize elements of an array 'n'
        Dim i, j As Integer

        For i = 0 To 10
            n(i) = i + 100 ' set element at location i to i + 100
        Next i
        ' output each array element's value


    End Sub

End Module
